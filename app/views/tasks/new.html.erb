<h1>タスクリスト新規作成ページ</h1>

<%= form_with(model: @task) do |f| %>
    <%= f.label :content, 'タスク' %>
    <%= f.text_field :content %>
    
    <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧へ戻る', tasks_path %>